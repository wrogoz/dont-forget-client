const initialState = {
    greetings:"redux working fine"
}
  const reducer = (state = initialState, action: { type: any; })=> {
      switch (action.type) {
       
        default:
          return state
      }
    }
  export default reducer;